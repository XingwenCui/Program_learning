#include<stdio.h>  //1.tell complier to import a stdio.h .2. And std is standard, io is input and output
// # include is not a c statement.
int main(void) //main function, only have one main function, c canstart with main and end after main end
{
	int number;
	number=2014;
	printf("Hello ! \n"); // printf() is a method from C stdiolibrary
	printf("This is %d\n",number);
	return 0;
}

